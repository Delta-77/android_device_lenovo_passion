allow hal_lineage_livedisplay_qti init:unix_stream_socket { connectto } ;
allow hal_power_default sysfs:file { write open } ;
allow system_server init:binder { call } ;
allow system_server init:binder { transfer } ;
allow vfmService init:process { signull } ;
allow webview_zygote zygote:unix_dgram_socket { write } ;
allow kernel kernel:capability { dac_override } ;

